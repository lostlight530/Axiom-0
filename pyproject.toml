[project]
name = "axiom-reference"
version = "0.1.0"
description = "Dependency-free reference contracts for the Axiom ten-stage pipeline"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.coverage.run]
branch = true
source = ["CODE"]